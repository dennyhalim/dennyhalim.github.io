# =============================================================================
# linkyee — Site Configuration
#
# This file is rendered through Liquid: every {{ ... }} below is evaluated
# at build time. Plugin output lives under `vars.<PluginName>` — see
# `plugins/README.md` for the full plugin developer wiki.
# =============================================================================

# Theme — directory under ./themes/. Built-ins:
#   default | minimal-mono | editorial-serif | neo-brutalism
#   glassmorphism | terminal-retro | paper-card | newsprint
theme: glassmorphism

# HTML <html lang> attribute
lang: "en"

# Force a color scheme regardless of the visitor's system appearance:
#   auto  — follow OS (default; the page auto-switches between light/dark)
#   light — always render light mode
#   dark  — always render dark mode
# Note: dark-first themes (e.g. terminal-retro) ignore "light" — their
# whole identity is the dark palette.
color_scheme: auto

# -----------------------------------------------------------------------------
# Plugins — every built-in plugin is shown below as a working example.
# Remove the ones you don't need. Output is referenced as {{ vars.PluginName }}.
# Full reference: plugins/README.md
#
# ⚠️ SECRETS — never put API keys / tokens in this file. config.yml is
# committed to git and rendered into the publicly served site. If a plugin
# needs auth (a personal access token, Steam Web API key, weather API
# token, ...), set it as a GitHub Actions repository secret
# (Settings → Secrets and variables → Actions → New repository secret),
# expose it to the build via `env:` in `.github/workflows/build.yml`,
# and read it from the plugin with ENV['MY_SECRET']. See plugins/README.md
# for the canonical pattern.
# -----------------------------------------------------------------------------
plugins:
  # Star counts for one or more GitHub repos.
  # Use as: {{ vars.GithubRepoStarsCountPlugin['owner/repo'] }}
  - GithubRepoStarsCountPlugin:
      - dennyhalim/cfg
      - dennyhalim/alkitabiah

  # Latest commit (sha / date / message) for a repo. Use as:
  #   {{ vars.GithubLastCommitPlugin['owner/repo'].date }}
  #   {{ vars.GithubLastCommitPlugin['owner/repo'].sha }}
  - GithubLastCommitPlugin:
      - dennyhalim/cfg

  # Public profile stats (followers / following / public_repos). Use as:
  #   {{ vars.GithubProfilePlugin['user'].followers }}
  - GithubProfilePlugin:
      - dennyhalim

  # Latest items from any RSS / Atom feed (Medium, blogs, YouTube channels,
  # podcasts...). Returns an array of { title, url, date } per feed URL.
  # Iterate inside theme index.html with {% for item in vars.RSSFeedPlugin[...] %}.
  - RSSFeedPlugin:
      - https://denny.wordpress.com/feed
      - https://dennyhalim.blogspot.com/feeds/posts/default
      - https://sabas.pages.dev/?format=rss

  # Days until / since a target date. Hash-style — labels become keys.
  # Use as: {{ vars.CountdownPlugin.label.days }}, .target, .passed
#  - CountdownPlugin:
#      linkyee_release: "2024-12-21"

  # Latest video for one or more YouTube channels (channel id, @handle, or URL).
  # Use as: {{ vars.YouTubeChannelLatestVideoPlugin['@handle'].title }}
  #         {{ vars.YouTubeChannelLatestVideoPlugin['@handle'].url }}
  - YouTubeChannelLatestVideoPlugin:
      - "@alkitabsaja"

# -----------------------------------------------------------------------------
# Tracking
# -----------------------------------------------------------------------------
#google_analytics_id:

# -----------------------------------------------------------------------------
# Profile header
# -----------------------------------------------------------------------------
title: "denny's"
avatar: "https://cdn5.telesco.pe/file/TA1T0y9LP70hT3rhWWQt9h_5in-fWi8sGq0Wd6e2M-fhhThFZ_GE6HPNWEnhTdOK2OJQWDdfRHcvIgXWzirTY4TQki6C4RolVWIKFPwtMuyKCKzkLAZEmJy-MmPx-WL80YDC8L-jrpMF3wxcWFiU_ed7-qYTdRtn-0okPYCbmkU1TzNDx9WJBgB53bvol-8Oj1bdzBcM1_KG4rzjqYjfSk6r_ERcz6XM_yIaNMNZvDGJqhvQNzZtczZzJcMd2K-EuVssxu9CeRLmlcSynPFSs7-ahgd0BMFg-fcmh8UkfDbGFVaHDdcB_l15Z-tJNIBxACHKy7F3sa8iOrTPQZ9m1Q.jpg"
name: "@dennyhalim"
tagline: >-
    father, husband, technophile
#    Followed by {{ vars.GithubProfilePlugin['ZhgChgLi'].followers }} on GitHub across {{ vars.GithubProfilePlugin['ZhgChgLi'].repos }} public repos.

# -----------------------------------------------------------------------------
# Links
#   icon  — Font Awesome class (https://fontawesome.com/search?o=r&m=free)
#   text  — displayed text (Liquid + HTML allowed)
#   title — link title attribute
#   url   — destination
#   alt   — accessibility text
#   target — `_blank` (new tab) or `_self`
# -----------------------------------------------------------------------------
links:
  - link:
      icon: "fa-solid fa-cart-shopping"
      text: "domains for SALE!"
      url: "https://www.dynadot.com/user/mypolaris"
      target: "_blank"

  - link:
      icon: "fa-solid fa-wordpress"
      text: "Blog"
      url: "https://denny.wordpress.com"
      alt: "wordpress"
      title: "wordpress"
      target: "_blank"

  - link:
      icon: "fa-solid fa-screwdriver-wrench"
      text: "Polaris Network"
      url: "https://www.mypolaris.com/"
      target: "_blank"

  - link:
      icon: "fa-solid fa-blogger"
      text: "Blogspot <span class='link-button-text'>(120+ Posts)</span>"
      url: "https://dennyhalim.blogspot.com"
      target: "_blank"

  - link:
      icon: "fa-solid fa-blogger"
      text: "Tips Blogspot <span class='link-button-text'>(120+ Posts)</span>"
      url: "https://komputips.blogspot.com"
      target: "_blank"

  - link:
      icon: "fa-solid fa-rss"
      text: "Wikidot <span class='link-button-text'>(120+ Posts)</span>"
      url: "https://dennyhalim.wikidot.com"
      target: "_blank"

  # → uses GithubRepoStarsCountPlugin
#  - link:
#      icon: "fa-brands fa-swift"
#      text: "<span class='link-button-text'>({{vars.GithubRepoStarsCountPlugin['dennyhalim/cfg']}} Stars)</span>"
#      url: "https://github.com/dennyhalim/cfg"
#      alt: " is a pure-Swift library that helps you to convert HTML strings to NSAttributedString with customized style and tags."
#      title: " is a pure-Swift library that helps you to convert HTML strings to NSAttributedString with customized style and tags."
#      target: "_blank"


  # → uses RSSFeedPlugin (latest blog post). `latest: true` opts into the
  #   theme's "LATEST" badge styling.
  - link:
      icon: "fa-solid fa-newspaper"
      text: "{{ vars.RSSFeedPlugin['https://denny.wordpress.com/feed'][0].title }}"
      url: "{{ vars.RSSFeedPlugin['https://denny.wordpress.com/feed'][0].url }}"
      alt: "Latest blog post"
      title: "Latest blog post"
      target: "_blank"
      latest: true

  - link:
      icon: "fa-solid fa-newspaper"
      text: "{{ vars.RSSFeedPlugin['https://dennyhalim.blogspot.com/feeds/posts/default'][0].title }}"
      url: "{{ vars.RSSFeedPlugin['https://dennyhalim.blogspot.com/feeds/posts/default'][0].url }}"
      alt: "Latest blog post"
      title: "Latest blog post"
      target: "_blank"
      latest: true

  - link:
      icon: "fa-solid fa-cross"
      text: "{{ vars.RSSFeedPlugin['https://sabas.pages.dev/?format=rss'][0].title }}"
      url: "{{ vars.RSSFeedPlugin['https://sabas.pages.dev/?format=rss'][0].url }}"
      alt: "Latest blog post"
      title: "Latest blog post"
      target: "_blank"
      latest: true

  # → uses YouTubeChannelLatestVideoPlugin
  - link:
      icon: "fa-brands fa-youtube"
      text: "{{ vars.YouTubeChannelLatestVideoPlugin['@alkitabsaja'].title }}"
      url: "{{ vars.YouTubeChannelLatestVideoPlugin['@alkitabsaja'].url }}"
      alt: "Latest YouTube video"
      title: "Latest YouTube video"
      target: "_blank"
      latest: true

# -----------------------------------------------------------------------------
# Social media buttons
# -----------------------------------------------------------------------------
socials:
  - social:
      icon: "fa-brands fa-medium"
      url: "https://denny.wordpres.com"
      title: "wordpress"
      alt: "wordpress"
      target: "_blank"
  - social:
      icon: "fa-brands fa-github"
      url: "https://github.com/dennyhalim"
      title: "GitHub"
      alt: "GitHub"
      target: "_blank"
  - social:
      icon: "fa-brands fa-twitter"
      url: "https://twitter.com/dennyhalim"
      title: "Twitter"
      alt: "Twitter"
      target: "_blank"
  - social:
      icon: "fa-brands fa-instagram"
      url: "https://www.instagram.com/alkitabsaja/"
      title: "Instagram"
      alt: "Instagram"
      target: "_blank"
#  - social:
#      icon: "fa-solid fa-envelope"
#      url: "mailto:zhgchgli@gmail.com"
#      title: "Email: zhgchgli@gmail.com"
#      alt: "zhgchgli@gmail.com"
#      target: "_blank"

# -----------------------------------------------------------------------------
# Footer (uses CountdownPlugin)
# -----------------------------------------------------------------------------
footer: >
    Hi there! Follow me on my blogs, or stay connected on social media.
#    <br>linkyee has been live for {{ vars.CountdownPlugin.linkyee_release.days }} days. 🎉

# Footer copyright message
# Linkyee is a 100% free, open-source project—feel free to modify the copyright message as you like. :)
copyright: >
  © {{ year }} <a href="https://denny.wordpress.com" target="_blank">denny</a>.
#  Powered by <a href="https://github.com/ZhgChgLi/linkyee" target="_blank">linkyee</a>
