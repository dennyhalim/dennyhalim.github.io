// newsprint theme — no JS needed. Hover effects + auto dark-mode are
// handled entirely by CSS. File kept so scaffold.rb's copy step matches
// the contract that every theme directory must contain scripts.js.
